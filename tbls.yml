docPath: doc/${SCHEMA} # ドキュメントの出力先
dsn: mysql://root:@localhost:3306/${DATABASE} # 対象のDBのDSN
detectVirtualRelations: # カラム名からリレーションを探してくれる
  enabled: true
  strategy: singularTableName
disableOutputSchema: true # スキーマ情報をjsonとして出力しない
